public class Currency {
    private String id;

    public Currency(String id) {
        setId(id);
    }

    private void setId(String id) {
            this.id = id;
        }

    public String getId() {
            return id;
        }
}
